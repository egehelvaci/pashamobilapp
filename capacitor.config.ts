import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pasha.mobilwebview',
  appName: 'Pasha MobilWebView',
  webDir: 'dist'
};

export default config;
